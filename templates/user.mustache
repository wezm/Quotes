<h2>{{username}}</h2>
<img src="{{avatar}}" alt="{{name}}'s avatar" />

<div class="vcard">
  <div class="field">Name: <span class="fn">{{name}}</span></div>
  <div class="field">Email: <span class="email">{{email}}</span></div>
</div>

<div class="quote-list">
  <h3>Quotes</h3>
  <ul>
    {{#quotes}}
    <li>
      <blockquote cite="/users/{{quotee}}">
        <p><span class="left quote">&ldquo;</span>{{body}}<span class="right quote">&rdquo;</span></p>
        &mdash; <a href="/users/{{quotee}}" class="username">{{quotee}}</a>{{#created_at}}, {{date}}{{/created_at}}
      </blockquote>
    </li>
    {{/quotes}}
  </ul>
</div>

{{{pager}}}